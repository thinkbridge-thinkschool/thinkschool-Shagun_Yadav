// Matches thinkschool_Shagun_Yadav/day-1/piece3/QuotesApi/Models/Quote.cs exactly -
// verified live against the running API. No createdAt, no other fields.
export interface Quote {
  id: number;
  author: string;
  text: string;
}
