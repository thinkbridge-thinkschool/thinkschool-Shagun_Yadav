{
  "Security": {
    "_comment": "Local development only — synthetic placeholder, never a real secret. Send it as the X-Api-Key header when calling the API locally.",
    "ApiKey": "dev-local-only-not-a-secret",
    "Jwt": {
      "_comment": "Local development only — synthetic placeholder, never a real secret. Signs/validates the tokens POST /api/v1/auth/register and /api/v1/auth/login issue (ADR-001, day-28/piece1). At least 32 bytes for HMAC-SHA256.",
      "SigningKey": "dev-local-only-jwt-signing-key-not-a-real-secret-32-bytes-min"
    }
  }
}
